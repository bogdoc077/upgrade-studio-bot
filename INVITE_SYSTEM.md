# Система запрошень з підтвердженням

## Огляд

Нова система дозволяє користувачам приєднуватися до приватних каналів та чатів через спеціальні посилання, які потребують підтвердження. Це забезпечує контрольований доступ тільки для користувачів з активною підпискою.

## Як працює система

### 1. Після оплати підписки
Користувач отримує повідомлення з кнопками для приєднання:
- 🔒 Приєднатися до каналу
- 💬 Приєднатися до чату

### 2. Натискання кнопки
- Бот перевіряє активність підписки
- Генерує або отримує invite посилання з бази даних
- Надсилає приватне посилання користувачу

### 3. Запит на приєднання
- Користувач переходить за посиланням
- Надсилає запит на приєднання
- Бот автоматично схвалює запити від користувачів з активною підпискою
- Відхиляє запити від користувачів без підписки

## Адміністрування

### Створення invite посилань

```bash
/create_invite <chat_id> <chat_type> <invite_link> [назва]
```

**Приклад:**
```
/create_invite -1002747224769 channel https://t.me/+AbCdEfGhIjKl Приватний канал
```

**Параметри:**
- `chat_id` - ID каналу/чату (отримати через `/get_chat_info`)
- `chat_type` - тип: `channel` або `group`
- `invite_link` - посилання з налаштуванням "потребує підтвердження"
- `назва` - опціональна назва для зручності

### Управління посиланнями

```bash
/manage_links    # Показати поточні посилання
/list_invites    # Детальний список всіх посилань
```

### Створення invite посилання в Telegram

1. Перейдіть до каналу/чату як адміністратор
2. Налаштування → Посилання для запрошення → Створити посилання
3. Увімкніть "Запит на приєднання" (потребує підтвердження)
4. Скопіюйте посилання
5. Додайте через `/create_invite`

## База даних

### Таблиця invite_links

| Поле | Тип | Опис |
|------|-----|------|
| id | Integer | Первинний ключ |
| chat_id | String | ID каналу/чату |
| chat_type | String | Тип: "channel" або "group" |
| chat_title | String | Назва каналу/чату |
| invite_link | String | Посилання для приєднання |
| is_active | Boolean | Чи активне посилання |
| created_at | DateTime | Час створення |
| updated_at | DateTime | Час останнього оновлення |

## Автоматична обробка запитів

Система автоматично:
- ✅ Схвалює запити від користувачів з активною підпискою
- ❌ Відхиляє запити від користувачів без підписки
- 📨 Надсилає повідомлення з результатом
- 📝 Логує всі дії для аудиту

## Безпека

- Invite посилання зберігаються в захищеній базі даних
- Доступ тільки для користувачів з активною підпискою
- Автоматична перевірка статусу підписки
- Логування всіх запитів на приєднання

## Переваги

1. **Контрольований доступ** - тільки платні користувачі
2. **Автоматизація** - без ручного підтвердження
3. **Масштабованість** - підтримка множини каналів/чатів
4. **Гнучкість** - легке додавання нових спільнот
5. **Безпека** - захищені посилання в базі даних