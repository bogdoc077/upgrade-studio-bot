# 🚀 Запуск проєкту в продакшні

## Покрокові команди для запуску всього проєкту

### 1. Підготовка середовища
```bash
# Перейти в папку проєкту
cd /Users/admin/upgrade-studio-bot

# Створити віртуальне середовище (якщо ще не створене)
python3 -m venv .venv

# Активувати віртуальне середовище
source .venv/bin/activate

# Встановити Python залежності
pip install -r requirements.txt
```

### 2. Запуск міграцій (один раз)
```bash
# Запустити міграції бази даних
python3 migrate_database.py
python3 migrate_admin_settings.py
```

### 3. Встановлення залежностей для адмін-панелі
```bash
# Перейти в папку адмін-панелі
cd admin-panel

# Встановити npm залежності
npm install

# Повернутися в корінь проєкту
cd ..
```

### 4. Запуск всіх сервісів

#### Варіант А: Автоматичний запуск (рекомендовано)
```bash
# Дати права на виконання скриптам
chmod +x ./start_all.sh ./stop_all.sh

# Запустити все одразу
./start_all.sh
```

#### Варіант Б: Ручний запуск (кожен сервіс в окремому терміналі)

**Термінал 1 - API сервер:**
```bash
cd /Users/admin/upgrade-studio-bot
source .venv/bin/activate
python3 start_api.py
```

**Термінал 2 - Webhook сервер:**
```bash
cd /Users/admin/upgrade-studio-bot
source .venv/bin/activate
python3 start_webhook.py
```

**Термінал 3 - Telegram бот:**
```bash
cd /Users/admin/upgrade-studio-bot
source .venv/bin/activate
python3 main.py
```

**Термінал 4 - Адмін-панель:**
```bash
cd /Users/admin/upgrade-studio-bot/admin-panel
npm run dev
```

### 5. Перевірка роботи сервісів

```bash
# Перевірити API
curl http://localhost:8000/health

# Перевірити адмін-панель
# Відкрити браузер: http://localhost:3000
```

### 6. Зупинка всіх сервісів

#### Якщо використовували автозапуск:
```bash
./stop_all.sh
```

#### Якщо запускали вручну:
- Натисніть `Ctrl+C` в кожному терміналі

### 7. Логи та діагностика

```bash
# Переглянути логи (якщо використовували автозапуск)
tail -f logs/api.log
tail -f logs/webhook.log  
tail -f logs/bot.log
tail -f logs/admin_panel.log

# Перевірити які процеси запущені
ps aux | grep python
ps aux | grep node
```

## 📋 Швидкий чеклист

- [ ] Віртуальне середовище активоване
- [ ] Python залежності встановлені
- [ ] npm залежності встановлені в admin-panel
- [ ] Міграції БД запущені
- [ ] Всі ENV змінні налаштовані
- [ ] MySQL сервер запущений
- [ ] API сервер працює (порт 8000)
- [ ] Адмін-панель працює (порт 3000)
- [ ] Telegram бот онлайн

## ⚡ Команди одним рядком

```bash
# Повний запуск з нуля
cd /Users/admin/upgrade-studio-bot && source .venv/bin/activate && chmod +x ./start_all.sh ./stop_all.sh && ./start_all.sh
```

```bash
# Зупинка
./stop_all.sh
```